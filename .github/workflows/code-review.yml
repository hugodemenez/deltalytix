name: Cursor CLI Automation

on:
  # Run on pull requests
  pull_request:
    types: [opened, synchronize, reopened]
  # Run on push to main branch
  push:
    branches:
      - beta
  # Allow manual triggering
  workflow_dispatch:
    inputs:
      task:
        description: 'Task to perform'
        required: true
        default: 'code-review'
        type: choice
        options:
          - code-review
          - update-docs

jobs:
  cursor-automation:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch full history for better context
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install Cursor CLI
        run: |
          curl https://cursor.com/install -fsS | bash
          echo "$HOME/.cursor/bin" >> $GITHUB_PATH

      - name: Verify Cursor CLI installation
        run: |
          agent --version || echo "Agent command not found, checking path..."
          which agent || echo "Agent not in PATH yet"

      - name: Code Review (on PR)
        if: github.event_name == 'pull_request'
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
        run: |
          agent -p --model auto --output-format text \
            "Review the changes in this pull request. Focus on:
            - Code quality and best practices
            - Potential bugs or security issues
            - TypeScript/React best practices
            - Performance considerations
            - Missing error handling
            
            Provide a concise review summary. If there are critical issues, mention them clearly." \
            > code-review.txt
          
          echo "## Code Review Results" >> $GITHUB_STEP_SUMMARY
          cat code-review.txt >> $GITHUB_STEP_SUMMARY
          
          # Post review as PR comment
          gh pr comment ${{ github.event.pull_request.number }} \
            --body "## 🤖 Automated Code Review
            
            $(cat code-review.txt)
            
            ---
            *Generated by Cursor CLI workflow*"

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cursor-results
          path: |
            code-review.txt
          retention-days: 7
