name: Cursor CLI Automation

on:
  # Run on pull requests
  pull_request:
    types: [opened, synchronize, reopened]
  # Run on push to main branch
  push:
    branches:
      - beta
  # Allow manual triggering
  workflow_dispatch:
    inputs:
      task:
        description: 'Task to perform'
        required: true
        default: 'code-review'
        type: choice
        options:
          - code-review
          - update-docs

# Serialize runs per PR/branch so two reviews can't race and both post a
# fresh comment; the latest push supersedes any in-flight review.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  cursor-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch full history for better context
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install Cursor CLI
        run: |
          curl https://cursor.com/install -fsS | bash
          echo "$HOME/.cursor/bin" >> $GITHUB_PATH

      - name: Verify Cursor CLI installation
        run: |
          agent --version || echo "Agent command not found, checking path..."
          which agent || echo "Agent not in PATH yet"

      - name: Generate PR diff file
        if: github.event_name == 'pull_request'
        run: |
          set -euo pipefail

          DIFF_FILE="$GITHUB_WORKSPACE/pr.diff"
          DIFF_STAT_FILE="$GITHUB_WORKSPACE/pr.diff.stat"

          if git rev-parse HEAD^2 >/dev/null 2>&1; then
            git diff --stat HEAD^1...HEAD^2 > "$DIFF_STAT_FILE"
            git diff --find-renames HEAD^1...HEAD^2 > "$DIFF_FILE"
          else
            git fetch --no-tags origin \
              "${{ github.event.pull_request.base.sha }}" \
              "${{ github.event.pull_request.head.sha }}"
            git diff --stat "${{ github.event.pull_request.base.sha }}...${{ github.event.pull_request.head.sha }}" > "$DIFF_STAT_FILE"
            git diff --find-renames "${{ github.event.pull_request.base.sha }}...${{ github.event.pull_request.head.sha }}" > "$DIFF_FILE"
          fi

          if [ ! -s "$DIFF_FILE" ]; then
            echo "No PR diff was generated at $DIFF_FILE"
            exit 1
          fi

          echo "PR_DIFF_FILE=$DIFF_FILE" >> "$GITHUB_ENV"
          echo "PR_DIFF_STAT_FILE=$DIFF_STAT_FILE" >> "$GITHUB_ENV"

          echo "## PR Diff Stat" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          cat "$DIFF_STAT_FILE" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

      - name: Code Review (on PR)
        if: github.event_name == 'pull_request'
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          agent -p --model auto --trust --output-format text \
            "Review the changes in this pull request using the diff file at: $PR_DIFF_FILE

            Read that diff file directly and base the review on it. Focus on:
            - Code quality and best practices
            - Potential bugs or security issues
            - TypeScript/React best practices
            - Performance considerations
            - Missing error handling

            Provide a concise review summary. If there are critical issues, mention them clearly." \
            > code-review.txt

          # Bail out clearly if the agent produced no usable review
          if [ ! -s code-review.txt ]; then
            echo "::error::Code review produced no output; nothing to post"
            exit 1
          fi

          echo "## Code Review Results" >> $GITHUB_STEP_SUMMARY
          cat code-review.txt >> $GITHUB_STEP_SUMMARY

          # Hidden marker lets us find and update this comment on later commits
          MARKER="<!-- cursor-code-review -->"
          PR_NUMBER="${{ github.event.pull_request.number }}"

          # Build the comment body
          {
            echo "$MARKER"
            echo "## 🤖 Automated Code Review"
            echo ""
            cat code-review.txt
            echo ""
            echo "---"
            echo "*Generated by Cursor CLI workflow (last updated for commit \`${{ github.event.pull_request.head.sha }}\`)*"
          } > comment-body.md

          # Collect the ids of every existing comment carrying our marker
          # (oldest first). jq runs per page, so each id prints on its own
          # line; we then read them into an array without piping into `head`
          # (which would SIGPIPE `gh` under `pipefail`).
          MARKER_IDS=()
          if ALL_IDS=$(gh api \
              "repos/${{ github.repository }}/issues/${PR_NUMBER}/comments" \
              --paginate \
              --jq ".[] | select(.body | contains(\"$MARKER\")) | .id"); then
            mapfile -t MARKER_IDS < <(printf '%s\n' "$ALL_IDS" | sed '/^$/d')
          else
            echo "::warning::Failed to query existing comments; will post a new comment"
          fi

          # Best-effort deletion of comments we no longer want around
          delete_comments() {
            for cid in "$@"; do
              [ -n "$cid" ] || continue
              echo "Deleting stale review comment #$cid"
              gh api --method DELETE \
                "repos/${{ github.repository }}/issues/comments/${cid}" \
                || echo "::warning::Failed to delete comment #$cid"
            done
          }

          # Update the first marker comment in place; if that succeeds, drop
          # any extra duplicates so the PR keeps exactly one review comment.
          # Send the body as JSON (jq --rawfile) so review content with
          # backticks, quotes or $() is passed safely, not via the shell.
          if [ "${#MARKER_IDS[@]}" -gt 0 ] \
             && jq -n --rawfile body comment-body.md '{body: $body}' \
                  | gh api --method PATCH \
                      "repos/${{ github.repository }}/issues/comments/${MARKER_IDS[0]}" \
                      --input - ; then
            echo "Updated existing review comment #${MARKER_IDS[0]}"
            delete_comments "${MARKER_IDS[@]:1}"
          else
            if [ "${#MARKER_IDS[@]}" -gt 0 ]; then
              echo "::warning::Could not update existing comment; replacing it"
            else
              echo "No existing review comment found; posting a new one"
            fi
            gh pr comment "$PR_NUMBER" --body-file comment-body.md
            delete_comments "${MARKER_IDS[@]}"
          fi

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cursor-results
          path: |
            code-review.txt
            pr.diff
            pr.diff.stat
          retention-days: 7
