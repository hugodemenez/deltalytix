{
  "name": "Deltalytix",
  "install": "npm install && npx prisma generate && sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y docker.io docker-compose-v2 ffmpeg postgresql postgresql-client",
  "start": "bash scripts/docker-bootstrap.sh",
  "ports": [
    {
      "name": "next",
      "port": 3000
    }
  ],
  "terminals": [
    {
      "name": "next-dev",
      "command": "npm run dev",
      "description": "Next.js development server"
    }
  ]
}
